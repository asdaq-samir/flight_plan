# src/vfr/pipeline.py's retrain stage -- the SageMaker-Training-Job-shaped
# half of what was one "pipeline" image.
-r ../src/requirements.txt   # vfr's own list
scikit-learn   # retrain(), imported inside it so the processing image never needs it
joblib         # the model artifact
