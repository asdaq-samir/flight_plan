# CI only (.github/workflows/ci.yml) -- what's needed to import and test
# the modules tests/ actually covers. Not a project-wide dependency file
# -- see DEVELOPMENT.md for why this project doesn't have one of those
# anymore.
#
# vfr's own dependencies come from src/requirements*.txt, so a new import
# in vfr is covered here the moment it is added there. Anything a test
# needs beyond vfr itself goes below: a test module importing something
# missing fails at *collection*, which takes the whole suite down rather
# than one test.
-r src/requirements-charts.txt   # vfr's own list, charts included -- tests/ covers both
pytest
ruff
pydantic    # vfr.narrative -- the agents' shared request model; the agents install it through their frameworks
pygeodesy   # TEST ONLY -- the independent implementation tests/test_geo.py pins vfr.geo against
