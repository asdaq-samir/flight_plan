# CI only (.github/workflows/ci.yml, test-nav-log-agent job) -- what's
# needed on top of the service's own requirements to run
# nav-log-agent/tests/. The same pattern as planning-service's own
# requirements-dev.txt: pytest, httpx and ruff have no reason to ship inside
# the image that runs this service.
-r requirements.txt
pytest
httpx   # starlette.testclient.TestClient's transport, for the /compare route
ruff
