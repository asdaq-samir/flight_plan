/**
 * The one text-input/select border style, applied consistently
 * everywhere a plain form field appears -- `RouteForm`, the Flight
 * Briefing page's aircraft picker, and every Playground/Account form
 * had each typed this same string out independently before this
 * constant existed. Composes with extra classes as needed (width,
 * text alignment, font -- whatever a given field needs beyond the
 * shared border/padding).
 */
export const FIELD_INPUT = "rounded border border-slate-300 px-2 py-1";
